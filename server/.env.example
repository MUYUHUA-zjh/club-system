# 复制本文件为 .env 并填入真实随机值（.env 已被 .gitignore 忽略，不会提交）
# 生成随机值示例：node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=请替换为一段足够长的随机字符串
CHECKIN_SECRET=请替换为另一段足够长的随机字符串
PORT=3001
